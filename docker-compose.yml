services:
  rel-ai:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - rel-ai-data:/app/data
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - PROXY_BASE_URL=${PROXY_BASE_URL}

volumes:
  rel-ai-data:
